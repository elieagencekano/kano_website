---
const currentYear = new Date().getFullYear();

const links = {
  main: [
    { label: "Nos solutions", href: "#solutions" },
    { label: "Notre ADN", href: "#adn" },
    { label: "Contact", href: "#contact" }
  ],
  legal: [
    { label: "Mentions légales", href: "#mentions" },
    { label: "Politique de confidentialité", href: "#privacy" }
  ]
};

const social = [
  { name: "LinkedIn", href: "#", icon: `<path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/>` }
];
---

<footer class="footer">
  <div class="footer-content">
    <div class="footer-grid">
      <div class="footer-brand">
        <img src="/logo-kano.svg" alt="KANO" class="logo-img" />
        <p class="brand-tagline">Votre partenaire digital à Angers</p>
        <p class="brand-desc">Nous créons des sites qui attirent, convainquent et transforment.</p>
        <div class="social-links">
          {social.map(s => (
            <a href={s.href} aria-label={s.name} class="social-link">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <Fragment set:html={s.icon} />
              </svg>
            </a>
          ))}
        </div>
      </div>

      <div class="footer-links">
        <div class="link-column">
          <h4>Navigation</h4>
          <ul>
            {links.main.map(link => (
              <li><a href={link.href}>{link.label}</a></li>
            ))}
          </ul>
        </div>

        <div class="link-column">
          <h4>Légal</h4>
          <ul>
            {links.legal.map(link => (
              <li><a href={link.href}>{link.label}</a></li>
            ))}
          </ul>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p class="copyright">© {currentYear} KANO. Tous droits réservés.</p>
      <p class="location">Angers, France 🇫🇷</p>
    </div>
  </div>
</footer>

<style>
.footer{position:relative;padding:80px 0 40px;background:linear-gradient(180deg,transparent 0%,rgba(10,15,22,.5) 100%);border-top:1px solid rgba(255,255,255,.08);z-index:1}
.footer-content{max-width:1380px;margin:0 auto;padding:0 32px}
.footer-grid{display:grid;grid-template-columns:1.2fr 2fr;gap:80px;margin-bottom:60px;align-items:start}
.footer-brand{display:flex;flex-direction:column;gap:8px;align-items:flex-start}
.brand-logo{height:100px;width:auto;margin-bottom:0;display:block;margin-left:-8px;margin-top:-35px}
.brand-tagline{font-size:1.1rem;font-weight:600;color:var(--kano-light);margin:0;opacity:.9}
.brand-desc{font-size:.95rem;color:var(--kano-light);opacity:.7;line-height:1.6;margin:0;max-width:320px}
.social-links{display:flex;gap:12px;margin-top:8px}
.social-link{width:40px;height:40px;border-radius:50%;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);display:flex;align-items:center;justify-content:center;color:var(--kano-light);transition:all .3s cubic-bezier(.4,0,.2,1)}
.social-link:hover{background:rgba(212,175,55,.15);border-color:rgba(212,175,55,.3);color:var(--kano-gold);transform:translateY(-2px)}
.footer-links{display:grid;grid-template-columns:repeat(2,1fr);gap:40px}
.link-column h4{font-family:Montserrat,sans-serif;font-size:.9rem;font-weight:700;color:#fff;text-transform:uppercase;letter-spacing:1px;margin:0 0 20px}
.link-column ul{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:12px}
.link-column a{color:var(--kano-light);font-size:.95rem;text-decoration:none;opacity:.8;transition:all .25s}
.link-column a:hover{color:var(--kano-gold);opacity:1;padding-left:4px}
.footer-bottom{display:flex;justify-content:space-between;align-items:center;padding-top:40px;border-top:1px solid rgba(255,255,255,.08)}
.copyright,.location{font-size:.9rem;color:var(--kano-light);opacity:.6;margin:0}
@media (max-width:768px){
  .footer{padding:60px 0 30px}
  .footer-content{padding:0 20px}
  .footer-grid{grid-template-columns:1fr;gap:40px;margin-bottom:40px}
  .footer-links{grid-template-columns:1fr;gap:32px}
  .footer-bottom{flex-direction:column;gap:12px;text-align:center;padding-top:30px}
}
</style>