---
---

<section class="hero-adn" aria-label="Notre ADN">
  <div class="container">
    <h1>
      <span class="line1">On a créé KANO parce qu'autour de nous,</span>
      <span class="line2">on voyait trop d'entreprises perdues.</span>
    </h1>
    <p class="tagline">Perdues dans leur relation au digital. Pas par manque d'envie. Pas par manque de moyens. Simplement parce que personne ne leur expliquait vraiment.</p>
  </div>
  
  <div class="scroll-indicator">
    <span>Découvrir notre histoire</span>
    <div class="scroll-arrow">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 5v14M5 12l7 7 7-7"/>
      </svg>
    </div>
  </div>
</section>

<style>
.hero-adn{position:relative;min-height:100svh;display:flex;flex-direction:column;justify-content:center;align-items:center;padding:120px 0 80px;overflow:hidden}
.container{max-width:1000px;margin:0 auto;padding:0 32px;text-align:center}
h1{font-family:Montserrat,sans-serif;font-weight:900;margin:0 0 28px;line-height:1.15}
.line1{display:block;font-size:clamp(1.6rem,3.2vw,2.4rem);color:#fff;opacity:0;animation:slideUp 1200ms cubic-bezier(.16,1,.3,1) 200ms forwards}
.line2{display:block;font-size:clamp(2.2rem,4.4vw,3.6rem);background:linear-gradient(135deg,var(--kano-gold) 0%,#f5d76e 100%);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;padding-bottom:6px;opacity:0;animation:slideUp 1200ms cubic-bezier(.16,1,.3,1) 500ms forwards}
@keyframes slideUp{from{opacity:0;transform:translateY(60px) scale(.92)}to{opacity:1;transform:translateY(0) scale(1)}}
.tagline{font-size:clamp(1.05rem,1.4vw,1.3rem);color:var(--kano-light);opacity:0;line-height:1.7;margin:0;max-width:650px;margin-left:auto;margin-right:auto;animation:fadeUp 1000ms cubic-bezier(.16,1,.3,1) 900ms forwards}
@keyframes fadeUp{from{opacity:0;transform:translateY(40px)}to{opacity:.8;transform:translateY(0)}}
.scroll-indicator{position:absolute;bottom:40px;left:50%;transform:translateX(-50%);display:flex;flex-direction:column;align-items:center;gap:12px;opacity:0;animation:fadeInCenter 800ms cubic-bezier(.16,1,.3,1) 1800ms forwards}
@keyframes fadeInCenter{from{opacity:0;transform:translateX(-50%)}to{opacity:1;transform:translateX(-50%)}}
.scroll-indicator span{font-size:.85rem;color:var(--kano-light);opacity:.6;font-weight:500;letter-spacing:.5px}
.scroll-arrow{width:44px;height:44px;border-radius:50%;border:1px solid rgba(212,175,55,.25);background:rgba(212,175,55,.05);display:flex;align-items:center;justify-content:center;color:var(--kano-gold);animation:float 3s ease-in-out infinite;transition:all .3s cubic-bezier(.34,.46,.35,1)}
.scroll-arrow:hover{background:rgba(212,175,55,.12);border-color:rgba(212,175,55,.4);transform:scale(1.08)}
.scroll-arrow svg{transition:transform .3s cubic-bezier(.34,.46,.35,1)}
.scroll-arrow:hover svg{transform:scale(1.15) rotate(8deg)}
@keyframes float{0%,100%{transform:translateY(0)}50%{transform:translateY(10px)}}
@media (max-width:768px){.hero-adn{padding:100px 0 60px}.container{padding:0 24px}.scroll-indicator{bottom:30px}}
@media (max-width:480px){.hero-adn{padding:84px 0 42px}.line1{font-size:1.4rem}.line2{font-size:1.8rem}.tagline{font-size:1rem}.scroll-indicator{bottom:24px}.scroll-indicator span{font-size:.8rem}}
</style>