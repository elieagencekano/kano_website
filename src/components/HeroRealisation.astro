---
---

<section class="hero-realisations" aria-label="Nos réalisations">
  <div class="container">
    <h1>Nos réalisations</h1>
    <p class="intro">Des projets concrets, des résultats mesurables. Découvrez ce qu'on a construit pour nos clients.</p>
  </div>
</section>

<style>
.hero-realisations{position:relative;padding:160px 0 80px;overflow:hidden}
.container{max-width:900px;margin:0 auto;padding:0 32px;text-align:center}
h1{font-family:Montserrat,sans-serif;font-weight:900;font-size:clamp(2.2rem,4.4vw,3.6rem);margin:0 0 20px;background:linear-gradient(135deg,#fff 0%,rgba(255,255,255,.9) 100%);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;line-height:1.2;padding-bottom:4px;opacity:0;animation:slideUp 1000ms cubic-bezier(.16,1,.3,1) 200ms forwards}
.intro{font-size:clamp(1.05rem,1.4vw,1.25rem);color:var(--kano-light);opacity:0;line-height:1.7;margin:0;max-width:600px;margin-left:auto;margin-right:auto;animation:fadeUp 900ms cubic-bezier(.16,1,.3,1) 500ms forwards}
@keyframes slideUp{from{opacity:0;transform:translateY(40px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeUp{from{opacity:0;transform:translateY(30px)}to{opacity:.85;transform:translateY(0)}}
@media (max-width:768px){.hero-realisations{padding:120px 0 60px}.container{padding:0 24px}}
@media (max-width:480px){.hero-realisations{padding:100px 0 40px}h1{font-size:2rem}.intro{font-size:1rem}}
</style>