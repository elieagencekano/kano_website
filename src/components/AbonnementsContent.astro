---
const vitrineOffers = [
  {
    name: "Essentiel",
    price: "50",
    description: "Pour assurer le strict nécessaire et garantir un site stable, sécurisé et opérationnel.",
    features: [
      { text: "Hébergement performant en France", included: true },
      { text: "Site sécurisé en HTTPS, sauvegardes mensuelles externalisées", included: true },
      { text: "Maintenance technique : mise à jour WordPress, thèmes, extensions", included: true },
      { text: "Surveillance basique & anti-intrusion", included: true },
      { text: "Prestation webmarketing", included: false },
      { text: "Rapport mensuel personnalisé", included: false },
      { text: "Suggestions proactives", included: false },
      { text: "Priorité support", included: false }
    ],
    ideal: "Vous avez peu d'évolutions et souhaitez garder un site sain sans vous en occuper.",
    highlighted: false
  },
  {
    name: "Sérénité",
    price: "120",
    description: "Pour faire évoluer votre site régulièrement et maintenir une présence digitale active.",
    subtitle: "Objectif : plus de visibilité.",
    features: [
      { text: "Hébergement performant en France", included: true },
      { text: "Site sécurisé en HTTPS, sauvegardes mensuelles", included: true },
      { text: "Maintenance technique : WordPress, thèmes, extensions", included: true },
      { text: "Surveillance basique & anti-intrusion", included: true },
      { text: "1h de prestation webmarketing", included: true },
      { text: "Rapport mensuel personnalisé", included: false },
      { text: "Suggestions proactives", included: false },
      { text: "Priorité support", included: false }
    ],
    ideal: "Vous voulez un équilibre entre stabilité technique et dynamique de croissance.",
    highlighted: false
  },
  {
    name: "KANO+",
    price: "150",
    description: "Pour ceux qui veulent aller plus loin sans se soucier des détails techniques.",
    subtitle: "Objectif : plus de clients.",
    features: [
      { text: "Hébergement performant en France", included: true },
      { text: "Site sécurisé en HTTPS, sauvegardes mensuelles", included: true },
      { text: "Maintenance technique : WordPress, thèmes, extensions", included: true },
      { text: "Surveillance basique & anti-intrusion", included: true },
      { text: "2h de webmarketing", included: true },
      { text: "Rapport mensuel personnalisé", included: true },
      { text: "Suggestions proactives", included: true },
      { text: "Priorité support", included: true }
    ],
    ideal: "Vous voulez un suivi complet, créatif et flexible.",
    highlighted: false
  }
];

const ecommerceOffers = [
  {
    name: "Fondations",
    price: "80",
    description: "Pour garantir une boutique fiable, sécurisée et prête à encaisser les ventes 24/7.",
    features: [
      { text: "Hébergement performant en France", included: true },
      { text: "HTTPS + sauvegardes mensuelles", included: true },
      { text: "Maintenance technique WordPress + plugins e-commerce", included: true },
      { text: "Surveillance basique & anti-intrusion", included: true },
      { text: "1h de webmarketing", included: false },
      { text: "Rapport mensuel personnalisé", included: false },
      { text: "Suggestions proactives", included: false },
      { text: "Priorité support", included: false }
    ],
    ideal: "Vous lancez votre boutique et voulez de la stabilité.",
    highlighted: false
  },
  {
    name: "Conquête",
    price: "160",
    description: "Pour faire évoluer votre boutique et maintenir un rythme commercial régulier.",
    subtitle: "Objectif : plus de commandes.",
    features: [
      { text: "Tout ce du niveau Fondations", included: true },
      { text: "1h webmarketing", included: true },
      { text: "Rapport mensuel (ventes, trafic, performances)", included: false },
      { text: "Suggestions proactives", included: false },
      { text: "Priorité support", included: false }
    ],
    ideal: "Vous cherchez un bon équilibre entre technique et marketing.",
    highlighted: false
  },
  {
    name: "Performance",
    price: "200",
    description: "Pour accélérer vos ventes et maximiser les conversions.",
    subtitle: "Objectif : plus de rentabilité.",
    features: [
      { text: "Tout ce du niveau Conquête", included: true },
      { text: "Analyse complète du comportement client", included: true },
      { text: "2h webmarketing", included: true },
      { text: "Rapport mensuel (ventes, trafic, performances)", included: true },
      { text: "Suggestions proactives", included: true },
      { text: "Priorité support", included: true }
    ],
    ideal: "Vous voulez passer au niveau supérieur et fidéliser vos clients.",
    highlighted: false
  }
];
---

<section class="hero-abonnements" aria-label="Nos abonnements">
  <div class="container">
    <h1>Prenez soin de votre site. Faites-le évoluer.</h1>
    <p class="intro">Des offres pensées pour accompagner votre présence en ligne, en toute simplicité.</p>
  </div>
</section>

<section class="section-offers vitrine" aria-label="Offres site vitrine">
  <div class="container-offers">
    <h2 class="section-title">Sites vitrines & maintenance</h2>
    
    <p class="mobile-hint">← Glissez pour découvrir nos offres →</p>
    
    <div class="offers-wrapper">
      <div class="offers-grid">
        {vitrineOffers.map((offer) => (
          <div class={`offer-card glass ${offer.highlighted ? 'highlighted' : ''}`}>
            {offer.highlighted && <div class="badge-popular">Le plus populaire</div>}
            
            <div class="offer-header">
              <h3 class="offer-name">{offer.name}</h3>
              <div class="offer-price">
                <span class="price-amount">{offer.price}€</span>
                <span class="price-period">/mois</span>
              </div>
              <p class="offer-description">{offer.description}</p>
              {offer.subtitle && <p class="offer-subtitle">{offer.subtitle}</p>}
            </div>

            <ul class="features-list">
              {offer.features.map((feature) => (
                <li class={feature.included ? 'included' : 'excluded'}>
                  {feature.included ? (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                      <path d="M5 12L10 17L20 7"/>
                    </svg>
                  ) : (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                      <path d="M18 6L6 18M6 6l12 12"/>
                    </svg>
                  )}
                  <span>{feature.text}</span>
                </li>
              ))}
            </ul>

            <div class="offer-footer">
              <p class="ideal-text"><strong>Idéal si…</strong> {offer.ideal}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <div class="email-promo-box glass">
      <div class="email-promo-content">
        <h3 class="option-title option-title--small">En option</h3>
        <p class="offer-description">
  <span class="highlight-gold">Mise en place et gestion d’adresses email professionnelles</span>
  avec votre nom de domaine, incluant une protection antispam.
</p>

        <ul class="email-features">
          <li class="included">
             <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12L10 17L20 7"/></svg>
             <span>Filtrage antispam avancé</span>
          </li>
          <li class="included">
             <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12L10 17L20 7"/></svg>
             <span>Jusqu’à 5 adresses email incluses</span>
          </li>
          <li class="included">
             <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12L10 17L20 7"/></svg>
             <span>Configuration standard et support basique</span>
          </li>
        </ul>
      </div>
      <div class="email-promo-price-wrapper">
        <div class="offer-price">
            <span class="price-amount">50€</span>
            <span class="price-period">/mois</span>
        </div>
        <div class="sub-price">+8€ / adresse supplémentaire</div>
      </div>
    </div>
    </div>
</section>

<section class="section-offers ecommerce" aria-label="Offres e-commerce">
  <div class="container-offers">
    <h2 class="section-title">Boutiques e-commerce</h2>
    
    <p class="mobile-hint">← Glissez pour découvrir nos offres →</p>
    
    <div class="offers-wrapper">
      <div class="offers-grid">
        {ecommerceOffers.map((offer) => (
          <div class={`offer-card glass ${offer.highlighted ? 'highlighted' : ''}`}>
            {offer.highlighted && <div class="badge-popular">Le plus populaire</div>}
            
            <div class="offer-header">
              <h3 class="offer-name">{offer.name}</h3>
              <div class="offer-price">
                <span class="price-amount">{offer.price}€</span>
                <span class="price-period">/mois</span>
              </div>
              <p class="offer-description">{offer.description}</p>
              {offer.subtitle && <p class="offer-subtitle">{offer.subtitle}</p>}
            </div>

            <ul class="features-list">
              {offer.features.map((feature) => (
                <li class={feature.included ? 'included' : 'excluded'}>
                  {feature.included ? (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                      <path d="M5 12L10 17L20 7"/>
                    </svg>
                  ) : (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                      <path d="M18 6L6 18M6 6l12 12"/>
                    </svg>
                  )}
                  <span>{feature.text}</span>
                </li>
              ))}
            </ul>

            <div class="offer-footer">
              <p class="ideal-text"><strong>Idéal si…</strong> {offer.ideal}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
.hero-abonnements{position:relative;padding:160px 0 80px;overflow:hidden}
.container{max-width:900px;margin:0 auto;padding:0 32px;text-align:center}
h1{font-family:Montserrat,sans-serif;font-weight:900;font-size:clamp(2.2rem,4.4vw,3.6rem);margin:0 0 20px;background:linear-gradient(135deg,#fff 0%,rgba(255,255,255,.9) 100%);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;line-height:1.2;padding-bottom:4px;opacity:0;animation:slideUp 1000ms cubic-bezier(.16,1,.3,1) 200ms forwards}
.intro{font-size:clamp(1.05rem,1.4vw,1.25rem);color:var(--kano-light);opacity:0;line-height:1.7;margin:0;max-width:700px;margin-left:auto;margin-right:auto;animation:fadeUp 900ms cubic-bezier(.16,1,.3,1) 500ms forwards}
@keyframes slideUp{from{opacity:0;transform:translateY(40px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeUp{from{opacity:0;transform:translateY(30px)}to{opacity:.85;transform:translateY(0)}}

.section-offers{position:relative;padding:80px 0;overflow:hidden}
.container-offers{max-width:1380px;margin:0 auto;padding:0 32px}
.section-title{font-family:Montserrat,sans-serif;font-weight:900;font-size:clamp(1.8rem,3vw,2.5rem);text-align:center;margin:0 0 48px;background:linear-gradient(135deg,#fff 0%,rgba(255,255,255,.9) 100%);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;line-height:1.2}

.mobile-hint{display:none;text-align:center;font-size:.95rem;color:var(--kano-light);opacity:.6;margin-bottom:24px}

.offers-wrapper{width:100%;overflow:visible}
.offers-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;margin:0 auto;max-width:1200px}

.glass{background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);backdrop-filter:blur(12px);-webkit-backdrop-filter:blur(12px);box-shadow:0 12px 40px rgba(0,0,0,.3)}

.offer-card{position:relative;border-radius:20px;padding:32px 28px;display:flex;flex-direction:column;gap:24px;transition:all .35s cubic-bezier(.4,0,.2,1);min-height:600px}
.offer-card:hover{transform:translateY(-6px);box-shadow:0 20px 60px rgba(0,0,0,.4);border-color:rgba(212,175,55,.3)}

.offer-card.highlighted{border:2px solid rgba(212,175,55,.5);box-shadow:0 16px 50px rgba(212,175,55,.2)}
.offer-card.highlighted:hover{border-color:rgba(212,175,55,.7);box-shadow:0 24px 70px rgba(212,175,55,.3)}

.badge-popular{position:absolute;top:-12px;left:50%;transform:translateX(-50%);background:var(--kano-gold);color:var(--kano-ink);padding:6px 20px;border-radius:999px;font-size:.8rem;font-weight:700;white-space:nowrap;box-shadow:0 4px 16px rgba(212,175,55,.4)}

.offer-header{display:flex;flex-direction:column;gap:12px;padding-bottom:20px;border-bottom:1px solid rgba(255,255,255,.1)}

.offer-name{font-family:Montserrat,sans-serif;font-weight:800;font-size:1.5rem;color:#fff;margin:0}

.option-title{
  font-family:Montserrat,sans-serif;
  font-weight:800;
  font-size:1.5rem;
  color:#fff;
  margin:0 0 12px;
  line-height:1.2;
}

.option-title--small{
  font-size:1.4rem;
}

.offer-price{display:flex;align-items:baseline;gap:6px}
.price-amount{font-family:Montserrat,sans-serif;font-weight:900;font-size:2.5rem;color:#fff;line-height:1}
.price-period{font-size:1.1rem;color:var(--kano-light);opacity:.7;font-weight:600}

.offer-description{font-size:.95rem;color:var(--kano-light);opacity:.85;line-height:1.6;margin:0}

.offer-subtitle{font-size:.9rem;color:var(--kano-gold);font-weight:600;font-style:italic;margin:0}

.features-list{list-style:none;display:flex;flex-direction:column;gap:12px;flex:1;margin:0;padding:0}
.features-list li{display:grid;grid-template-columns:auto 1fr;gap:12px;align-items:start;font-size:.9rem;color:var(--kano-light);line-height:1.5}
.features-list li.included svg{color:var(--kano-gold);flex-shrink:0;margin-top:2px}
.features-list li.excluded{opacity:.4}
.features-list li.excluded svg{color:var(--kano-light);flex-shrink:0;margin-top:2px}

.offer-footer{display:flex;flex-direction:column;gap:16px;margin-top:auto;padding-top:20px;border-top:1px solid rgba(255,255,255,.1)}

.ideal-text{font-size:.88rem;color:var(--kano-light);opacity:.8;line-height:1.5;margin:0}
.ideal-text strong{color:#fff;font-weight:600}

/* STYLES POUR L'ENCADRÉ EMAIL */
.email-promo-box {
  max-width: 1200px;
  margin: 40px auto 0;
  padding: 32px 40px;
  border-radius: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 40px;
  transition: transform .35s cubic-bezier(.4,0,.2,1);
}
.email-promo-box:hover {
  transform: translateY(-4px);
  border-color: rgba(212,175,55,.3);
}

.email-promo-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  text-align: left;
}

.option-badge {
  display: inline-block;
  font-family: Montserrat, sans-serif;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  font-weight: 700;
  color: var(--kano-gold);
  border: 1px solid rgba(212,175,55, 0.4);
  padding: 4px 12px;
  border-radius: 6px;
  margin-bottom: 16px;
  background: rgba(212,175,55, 0.1);
}

.highlight-gold {
  color: var(--kano-gold);
  font-weight: 700;
}

.email-features {
  list-style:none;
  margin:0;
  padding:0;
  display:flex;
  flex-direction:column;
  gap:12px;
}
.email-features li {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 0.95rem;
  color: var(--kano-light);
}
.email-features li svg {
  color: var(--kano-gold);
  flex-shrink: 0;
}

.email-promo-price-wrapper {
  text-align: right;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: flex-end; /* Align right */
}

.sub-price {
  font-size: 0.9rem;
  color: var(--kano-gold);
  margin-top: 8px;
  font-weight: 500;
}

.email-promo-content .offer-description {
  margin-bottom: 20px;
}


@media (max-width:1024px){
  .offers-grid{grid-template-columns:1fr;gap:20px;max-width:500px}
  .offer-card{min-height:auto}
  .mobile-hint{display:none}
  
  /* Ajustement encadré tablette vertical */
  .email-promo-box {
    max-width: 500px;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
    gap: 24px;
  }
  .email-promo-price-wrapper {
    align-items: flex-start;
    text-align: left;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 12px;
    width: 100%;
    border-top: 1px solid rgba(255,255,255,0.1);
    padding-top: 20px;
  }
}

@media (max-width:768px){
  .hero-abonnements{padding:120px 0 60px}
  .container{padding:0 24px}
  .section-offers{padding:60px 0;overflow:visible}
  .container-offers{padding:0;max-width:100%;overflow:visible}
  .section-title{margin-bottom:32px;padding:0 20px}
  
  .offers-wrapper{overflow-x:auto;overflow-y:visible;-webkit-overflow-scrolling:touch;scrollbar-width:none;-ms-overflow-style:none;margin:0;padding:0;width:100%;position:relative}
  .offers-wrapper::-webkit-scrollbar{display:none}
  
  .mobile-hint{display:block;padding:0 20px;margin-bottom:20px}
  
  .offers-grid{display:flex;gap:20px;max-width:none;padding:60px 20px 70px 20px}
  .offers-grid::after{content:"";display:block;width:1px;flex-shrink:0}
  
  .offer-card{flex:0 0 auto;width:min(340px,88vw);min-height:auto}
  .offer-header{gap:10px}
  .offer-name{font-size:1.3rem}
  .price-amount{font-size:2.2rem}
  .offer-description{font-size:.9rem}
  .features-list{gap:10px}
  .features-list li{font-size:.88rem}

  /* Ajustement encadré mobile */
  .email-promo-box {
    margin: 0 20px; 
    padding: 28px 24px;
    width: auto;
  }
  .email-promo-price-wrapper {
    flex-direction: column;
    gap: 4px;
  }
}

@media (max-width:480px){
  .hero-abonnements{padding:100px 0 40px}
  h1{font-size:2rem}
  .intro{font-size:1rem}
  .offer-card{width:min(320px,85vw);padding:28px 24px}
  .offers-grid{padding:60px 16px 70px 16px}
  .offers-grid::after{width:1px}
  
  .email-promo-box {
    margin: 0 16px;
  }
}
</style>